<?xml version="1.0" encoding="utf-8"?>
<!-- Zero NuGet dependencies, on purpose. Everything here is in-box .NET 8:
     HttpClient, System.Text.Json, System.Xml.Linq, Regex.
     No restore, no supply chain to audit, CI is setup-dotnet + build + test. -->
<configuration>
  <packageSources>
    <clear />
  </packageSources>
</configuration>
