# One container runs everything: dashboard server + 6-hourly scan sweep.
FROM mcr.microsoft.com/dotnet/sdk:8.0 AS build
WORKDIR /app
COPY nuget.config .
COPY src/ src/
RUN dotnet publish src/Jobscan/Jobscan.csproj -c Release -o /out

FROM mcr.microsoft.com/dotnet/aspnet:8.0
WORKDIR /app
COPY --from=build /out .
COPY profiles/ profiles/
COPY web/ web/
COPY src/ src/
ENV SCAN_INTERVAL_HOURS=6
EXPOSE 8080
ENTRYPOINT ["dotnet", "jobscan.dll", "--daemon"]
